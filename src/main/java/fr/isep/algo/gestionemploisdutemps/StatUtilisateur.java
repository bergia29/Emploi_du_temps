package fr.isep.algo.gestionemploisdutemps;

import java.util.Map;

public class StatUtilisateur {

    // Calculer le taux d'occupation des salles (ex: en %)
    public double calculerOccupationSalles() {
        // à implémenter
        return 0;
    }

    // Calculer la charge horaire de chaque enseignant (ex: Map enseignant -> heures)
    public Map<String, Integer> calculerChargeEnseignants() {
        // à implémenter
        return null;
    }

    // Générer un rapport d'utilisation (en String)
    public String genererRapportUtilisation() {
        // à implémenter
        return null;
    }

    // Exporter les statistiques dans un format donné (txt, csv...)
    public void exporterStat(String format) {
        // à implémenter
    }
}